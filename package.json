{
  "name": "@nerima-games/mc-physics",
  "version": "0.1.0",
  "type": "module",
  "description": "Euler integration and AABB collision resolution for the nerima-games Minecraft-clone rebuild, with the foot-origin/body-centre Y convention made explicit in the type system. No external physics engine.",
  "homepage": "https://github.com/nerima-games/mc-physics#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nerima-games/mc-physics.git"
  },
  "bugs": {
    "url": "https://github.com/nerima-games/mc-physics/issues"
  },
  "author": "takeokunn",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.15.0"
  },
  "packageManager": "pnpm@9.15.0",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "files": [
    "index.ts",
    "domain",
    "tsconfig.base.json",
    "LICENSE",
    "README.md"
  ],
  "keywords": [
    "minecraft",
    "effect-ts",
    "typescript",
    "voxel",
    "physics",
    "aabb",
    "voxel-dda"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.build.json --pretty false && tsc -p tsconfig.test.json --pretty false",
    "lint": "oxlint --deny-warnings index.ts domain scripts test",
    "lint:fix": "oxlint --fix index.ts domain scripts test",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:deps": "tsx scripts/check-dependency-whitelist.ts",
    "api:check": "tsx scripts/api-lock.ts",
    "api:update": "tsx scripts/api-lock.ts --write",
    "verify": "pnpm typecheck && pnpm lint && pnpm check:deps && pnpm api:check && pnpm test"
  },
  "dependencies": {
    "effect": "^3.20.0"
  },
  "devDependencies": {
    "@effect/vitest": "^0.25.1",
    "@types/node": "^22.19.11",
    "@vitest/coverage-v8": "^3.2.4",
    "oxlint": "^0.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
