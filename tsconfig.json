{
  // Editor / language-server default project. Aliases tsconfig.test.json
  // (the widest check-only config: src + test + scripts + vitest.config.ts)
  // so opening any file in an editor resolves against the same options CI
  // uses for typecheck.
  "extends": "./tsconfig.test.json"
}
